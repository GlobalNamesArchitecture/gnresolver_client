require 'spec_helper'

describe GnresolverClient do
  it 'has a version number' do
    expect(GnresolverClient::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
